# firmware/kicad/tabletop_penning/ — placeholder

KiCad 8.0+ project for board v0 of `tabletop_penning`.

**Status**: empty (Phase E funding-gated).  See `../README.md` for
context and `firmware/doc/board_v0_tabletop_penning.md` for paper spec input.
