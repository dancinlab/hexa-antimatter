# firmware/kicad/atomic_clock/ — placeholder

KiCad 8.0+ project for board v0 of `atomic_clock`.

**Status**: empty (Phase E funding-gated).  See `../README.md` for
context and `firmware/doc/board_v0_atomic_clock.md` for paper spec input.
