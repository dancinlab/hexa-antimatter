# firmware/kicad/thrust_acquisition/ — placeholder

KiCad 8.0+ project for board v0 of `thrust_acquisition`.

**Status**: empty (Phase E funding-gated).  See `../README.md` for
context and `firmware/doc/board_v0_thrust_acquisition.md` for paper spec input.
