# firmware/kicad/pet_cyclotron/ — placeholder

KiCad 8.0+ project for board v0 of `pet_cyclotron`.

**Status**: empty (Phase E funding-gated).  See `../README.md` for
context and `firmware/doc/board_v0_pet_cyclotron.md` for paper spec input.
